import RegisterDialog from "@/Components/RegisterDialog/RegisterDialog";

const RegisterPage = () => {
    return (
        <RegisterDialog open={true}></RegisterDialog>
    )
}

export default RegisterPage;