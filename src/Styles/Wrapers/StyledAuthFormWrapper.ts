import styled from "@emotion/styled";

const StyledAuthFormWrapper = styled.div`
    display: flex;
    flex-direction: row;
    height: 50%;
    max-height: 170px;
    padding-right: 1.5vw;
    padding-left: 1.5vw;
    justify-content: space-between;
`

export default StyledAuthFormWrapper